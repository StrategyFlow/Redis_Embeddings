# Embeddings_for_Vector_Database
Test code for embeddings for the vector database. Currently set up to work with the generated JSON data and use that data to embed and then input into redis database.


I have code for both the generated JSON data and the AEG, but the file "Generated_Data_Embedding" is what I believe will be most relevant for the initial demo. Adding the AEG correctly into the database is my next step as well as figuring out how people should access the database.
